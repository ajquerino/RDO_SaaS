# HANDOFF — IndustrialOS (continuar em outra máquina)

> Este arquivo permite ao Claude Code retomar o projeto de onde paramos. Leia-o inteiro ao abrir o projeto numa máquina nova. Os documentos de referência estão na pasta acima (`../PRD.md`, `../ARQUITETURA.md`, `../MODELO_DADOS_POSTGRES.md`, `../DDL_POSTGRES.sql`, `../ROADMAP_SPRINTS.md`, `../ANALISE_DO_MVP.md`, `../AUDITORIA_DOCS_VS_MVP.md`, `../PERMISSOES.md`, etc.).

## O que é
SaaS multi-tenant de **Gestão Operacional de Obras Industriais** (montagem, caldeiraria, tubulação, pintura…). O **RDO** (Relatório Diário de Obra) é o módulo central. Evolução de um MVP validado (`github.com/ajquerino/RDO`, HTML/Apps Script) para arquitetura moderna. **Não escrever código antes de aprovar documentação; nunca supor regra de negócio — perguntar ao usuário.**

## Stack
- **Backend:** .NET 10 (Clean Architecture: Domain/Application/Infrastructure/Api), EF Core, JWT, Swagger, multi-tenant (Global Query Filter + TenantMiddleware). Em `backend/`.
- **Frontend:** React + TS + Vite + Tailwind + PWA, React Query + zustand, mobile-first. Em `frontend/`.
- **Banco:** PostgreSQL. **Armazenamento:** Cloudflare R2 (S3-compat). **PDF:** QuestPDF. **Import:** ClosedXML.

## Estado (Sprints)
- **0 Fundação** ✅ · **1 Auth/RBAC/Usuários** ✅ · **2 Clientes/Obras/EAP/importar cronograma** ✅ · **3 RDO núcleo + PDF** ✅
- **4 RDO parte 2** 🟡 — FEITO: fotos/vídeos (R2), catálogo de 61 funções (Direta/Indireta), clima por GPS, hora extra automática, seções (retrabalho, segurança, dificuldades, próximo dia, pendências, planejamento), PDF completo com todas as seções. **FALTA: Assinaturas (canvas — campo backend `rdos.Assinaturas` jsonb já existe), Offline-first (IndexedDB + fila de sync).**
- **4 (continuação)** ✅ Assinaturas (canvas, `SignaturePad` em Rdo.tsx) + PDF completo (todas as seções + horas via `JornadaCalculo`). **Só falta Offline.**
- **5 Aprovação por token + revisão** ✅ — `AprovacaoController` público (`[AllowAnonymous]`, ignora filtro de tenant): GET `/aprovacao/{token}` (resumo do RDO), GET `/aprovacao/{token}/pdf`, POST `/aprovacao/{token}/aprovar` (nome → Aprovado, consome token, grava assinatura do fiscal se enviada), POST `/aprovacao/{token}/revisao` (nome+motivo → RevisaoSolicitada, invalida token). Frontend: rota pública `/aprovacao/{token}` (`Aprovacao.tsx`, sem login) + no `Rdo.tsx` mostra o link para copiar/enviar ao fiscal, banner de revisão e botão "Reenviar" (o `Finalizar` incrementa `Revisao`). Campos novos em `rdos`: `MotivoRevisao`, `RevisadoPor`, `RevisadoEm` (migration `AprovacaoRevisao`). PDF do RDO extraído para `RdoPdfFactory` (usado pelas rotas autenticada e pública).
- **6 Dashboards/Curva S/Faróis/Pareto** ✅ — Backend já tinha `DashboardController` GET `/obras/{id}/dashboard` (avanço HH-ponderado, avanço por item, HH previsto x realizado, efetivo médio, farol, Pareto paralisações/retrabalho); adicionado `curvaS` (acumulado realizado HH x rampa linear prevista, % do HH total). Frontend: aba **Dashboard** (`Dashboard.tsx`) dentro da obra, com cartões, barras e Curva S em SVG puro (sem lib nova).
- **7 Medição/Faturamento/Custos** ✅ — Entidades: `CondicaoPagamento` (parcelas jsonb [{dias,pct?}]), `FaturamentoPlano` (por obra), `FaturamentoEvento` (marcos: mobilização, entrada, comissionamento… tipos/bases em string), `Medicao` (BM, nº sequencial por obra, token) + filhos owned `MedicaoItem`/`MedicaoParcela`. Migration `Sprint7Medicao` (à mão: .cs + .Designer + snapshot). Cálculo em `Domain/Services/MedicaoCalculo.cs` (medido_periodo = (%fim-%ini)·valor; medido_acum = %fim·valor; pct_financeiro = acum/contrato; parcelas da condição, vencimento = ate+dias). Controllers (RBAC PLAN/GES/ADM; apagar BM só ADM): `CondicoesPagamentoController` CRUD, `FaturamentoController` (GET/POST/PUT plano+eventos), `MedicoesController` (calcular sem salvar / criar+numerar+token / listar / obter / delete / PDF). %ini = %fim da última medição do item; %fim = avanço atual (`AvancoCalculo.PctItem`, igual ao dashboard). `PctFisico` ponderado por valor; resposta do calcular também traz `avancoHh` (HH-ponderado). PDF do boletim em `Infrastructure/Pdf/MedicaoPdf.cs` (`IMedicaoPdf`). Frontend: aba **Medição** (`Medicao.tsx`, só PLAN/GES/ADM) — plano de faturamento (eventos + condição, presets 21/42 · 21/35/42/60 · 100% à vista) e emitir Boletim (período → itens %ini→%fim/valor/medido, totais, parcelas/vencimentos → salvar → PDF) + lista de BMs.
- **8 Equipamentos/Produtividade/Documentos** ✅ — **Equipamentos**: catálogo por tenant (`Equipamento : BaseEntity`), `EquipamentosController` (GET p/ todos; POST/PUT/DELETE PLAN/GES/ADM); no RDO (`Rdo.tsx`) o campo Equipamento tem `<datalist>` do catálogo; aba **Equipamentos** no Home (`Equipamentos.tsx`, PLAN/GES/ADM). **Documentos**: `Documento` (padrão RdoMidia, não é BaseEntity; FK obra cascade; R2 via IStorage), `DocumentosController` (POST/GET `/obras/{id}/documentos`, DELETE `/documentos/{id}`; ver=PodeVerObra, gerir=PLAN/GES/ADM); aba **Documentos** no detalhe da obra. **Produtividade**: `DashboardController` ganhou bloco `produtividade` — HH direto×indireto (pela `FuncaoMaoObra.Categoria`, não classificado quando a função não está no catálogo) + efetivo por função (HH/pessoas). Cards+barra no `Dashboard.tsx`. Índices físicos kg/t/m²/HH deixados como TODO (dependem de regra do usuário — não inventados). Migration `Sprint8EquipDocs` à mão (.cs+Designer+snapshot).
- **9 Hardening & Go-live (técnico)** ✅ — **Auditoria**: entidade `Auditoria` (não-BaseEntity, TenantId nulável), gerada no `SaveChanges` do AppDbContext (via `IUsuarioAtual` lido do JWT) para create/update/delete de Obra/Cliente/Usuario/Rdo/Medicao/Equipamento/Documento; `Detalhe` guarda só NOMES de campos (sem dados sensíveis); `GET /api/v1/auditoria` (Admin, paginado). **Cache**: `IMemoryCache` no `GET /obras/{id}/dashboard` (~60s por obra; autorização roda sempre). **Erros/observabilidade**: `CorrelationIdMiddleware` (X-Correlation-Id + Serilog `FromLogContext`) e `ExceptionMiddleware` (ProblemDetails RFC7807, detalhe só em Dev). **Rate limiting**: nativo, política `auth` 10 req/min por IP em `/auth/login` e `/auth/refresh`. **LGPD**: `Usuario.ConsentimentoLgpd/ConsentimentoEm`, `GET /api/v1/lgpd/meus-dados` e `POST /api/v1/lgpd/consentimento`; soft-delete já no filtro global. **Planos (só modelo)**: entidade `Plano` (global) + `Tenant.PlanoId`; `GET/POST /api/v1/planos` (Admin) — **sem cobrança/gateway** (TODO). **Onboarding**: `POST /api/v1/tenants` (Admin) cria tenant+empresa+admin (NÃO há signup público). Frontend: abas **Auditoria** e **Planos** no Home (Admin). Migration `Sprint9Hardening` à mão. **Pendências p/ usuário: billing (provedor/planos/trial) e signup self-service — perguntar antes de implementar.**
- **Super-admin & Console de Plataforma** ✅ — o **SuperAdmin** (dono do SaaS) foi movido para FORA das empresas-cliente: `Tenant.EhSistema` (bool, migration `SuperAdminPlataforma`) marca um tenant reservado **"Plataforma"**; o `DbSeeder` cria esse tenant de sistema e coloca (ou **migra**) `super@demo.com` nele — por isso o super **não enxerga dado de nenhum tenant** pelo filtro global. `PlataformaController` (`api/v1/plataforma`, `[Authorize(SuperAdmin)]`, **único ponto cross-tenant, sempre com `.IgnoreQueryFilters()` explícito**): `GET/tenants` (lista empresas + contagens, exclui o de sistema), `GET/tenants/{id}` (uso: obras/RDOs/usuários), `PUT/tenants/{id}/status` (ativo|suspenso), `POST/tenants` (cria empresa+admin), `GET/POST/planos`, `GET/metricas`. Os antigos `PlanosController`/`TenantsController` foram **removidos** (consolidados aqui). `AuthController.Login` **bloqueia login de tenant suspenso** (403), exceto SuperAdmin. **Guardrail mantido: filtro global e TenantMiddleware intactos; nenhum endpoint de tenant vê outro tenant.** Frontend: `App.tsx` roteia SuperAdmin para `PlataformaConsole.tsx` (empresas + status + nova empresa + planos + métricas); as abas de tenant (Obras/…/Auditoria) continuam para os demais papéis (aba Planos saiu do Home).
- **Arquitetura de IA (plumbing, SEM IA real)** ✅ — **Outbox**: `EventoDominio : BaseEntity` (Tipo/AgregadoTipo/AgregadoId/Payload jsonb/Processado/ProcessadoEm), gravado na MESMA transação nos momentos-chave: `rdo_finalizado` (RdosController.Finalizar), `rdo_aprovado`/`revisao_solicitada` (AprovacaoController — `TenantId` vem do RDO por ser rota anônima), `medicao_emitida` (MedicoesController.Criar), `obra_criada` (ObrasController.Criar). Só persiste — nada processa (pronto p/ worker futuro). Migration `EventosDominio` à mão. **Abstração de IA**: `Application/Ia` — `IResumoIa` + records `RdoContexto`/`RdoServicoContexto`/`ResumoResultado`; `RdoContextoBuilder.Build(rdo,obra,itens[,categorias])` normaliza o RDO (efetivo total/direto/indireto, HH via JornadaCalculo, serviços via AvancoCalculo, paralisações, retrabalho, clima, ocorrências). **Stub por regras** `Infrastructure/Ia/ResumoIaRegras : IResumoIa` (texto determinístico, `Origem="regras"`, sem rede/sem pacote de IA); DI `AddScoped<IResumoIa, ResumoIaRegras>` com comentário de como plugar LLM depois. **Endpoint** `GET /rdos/{id}/resumo` (PodeVerObra) → `{texto, origem}`. Frontend: botão **"Resumo do dia"** no editor do RDO. **Para ligar IA real: criar `ResumoIaLlm : IResumoIa` e trocar o registro no DI — contrato e contexto já prontos.**
- **Aviso de limite de plano (só AVISA, NÃO bloqueia)** ✅ — helper `Api/Common/UsoPlanoCalc` conta obras (não deletadas) e usuários (ativos, não deletados) do tenant corrente (filtro global) vs limites do `Plano` via `Tenant.PlanoId` (null = ilimitado). `ObrasController.Criar` e `UsuariosController.Criar` **sempre criam** e devolvem `{ ..., aviso }` quando atinge o limite (msg "atingiu o limite de X … faça upgrade"). `GET /api/v1/uso-plano` (autenticado) → `{ nObras, limiteObras, nUsuarios, limiteUsuarios, plano }`. `PlataformaController GET /tenants` inclui `limiteObras`/`limiteUsuarios` (além de `nObras`/`nUsuarios`) p/ o super-admin ver uso vs limite. Frontend: selo `SeloPlano` (Obras/Usuários "12/50", amarelo no limite), banner de aviso ao criar obra, e uso/limite por empresa no `PlataformaConsole`. Sem migration (só leitura).
- **Console: editar/excluir empresas e planos** ✅ (na main) — `PlataformaController` ganhou `EditarEmpresa` (PUT `tenants/{id}`, nome+CNPJ, sincroniza `RazaoSocial`), `ExcluirEmpresa` (DELETE `tenants/{id}` = **soft delete**: `DeletadoEm`+status suspenso, reversível), `EditarPlano`/`ExcluirPlano` (`planos/{id}`; exclusão bloqueada se o plano estiver em uso). `Tenant` ganhou `Cnpj` + `DeletadoEm`. Front `PlataformaConsole` com edição inline, excluir e CRUD de planos. `AuthController.Login` bloqueia tenant excluído/suspenso.
- **Deploy de produção (Docker)** ✅ (na main) — `MigrateAsync` + `SeedSistemaAsync` rodam em **TODOS os ambientes**; `SeedDemoAsync` só em Development (DbSeeder dividido). `appsettings.Local.json` optional. Arquivos: `.env.example`, `DEPLOY.md`, `docker-compose.prod.yml` (postgres+api+frontend), `backend/Dockerfile`, `frontend/Dockerfile` (nginx SPA + no-cache do sw), `.dockerignore`, `ops/` script de backup (`pg_dump -Fc`). `CriarTenant` semeia as **61 funções** na empresa nova. Super-admin do seed vem de `Seed:SuperAdminEmail/Senha` (fallback `super@demo.com`/`super123`).
- **Fechamento semanal de HH** ✅ (na main) — `Domain/Services/HhSemanalCalculo` + `GET /obras/{id}/hh-semanal?de=` (normaliza p/ segunda; baldes Normal/50/70/100/150 + `porDia`; respeita tenant e permissão por obra). Aba **"HH da semana"** na obra. ⚠️ Nesta versão da main as **taxas são FIXAS no código**; a versão configurável está no branch `feat/regras-he-por-empresa` (ver abaixo).
- **Recuperação de senha por e-mail** ✅ (na main) — `Application/Email/IEmailSender` + `Infrastructure/Email/EmailSender` (Resend/SendGrid por `Email:Provider`; **fallback de log em dev** quando `Email:ApiKey` vazio). Entidade `RedefinicaoSenha` (só hash SHA-256, 1h, uso único — migration). `AuthController` POST `/auth/esqueci-senha` (SEMPRE 200 neutro) e `/auth/redefinir-senha` (mín. 6 chars, invalida os demais tokens). Front: link no Login + rota pública `/redefinir-senha/:token`. Config: `Email:*` + `App:BaseUrl` (chave real só no `appsettings.Local.json`).
- **Billing / inadimplência (SOMENTE-LEITURA, SEM gateway)** ✅ (na main) — Entidade `Assinatura` (1 por tenant; campos de provedor nullable p/ o futuro) + `Domain/Services/AssinaturaCalculo` (estados SemAssinatura/Trial/EmDia/PrestesAVencer/Vencido/Bloqueada; tolerância 7d; avisos 7/3/1; **default seguro: sem assinatura nunca bloqueia**). Enforcement: `SomenteLeituraInadimplenteFilter` (global) — escrita de tenant bloqueado → **402**; GET/HEAD/OPTIONS, SuperAdmin e rotas `auth`/`plataforma`/`assinatura` sempre passam. `GET /assinatura/minha`; no `PlataformaController`: `GET /assinaturas`, `PUT tenants/{id}/assinatura` (upsert), `POST .../assinatura/pagar` (+1 mês). `CriarTenant` cria **trial de 14 dias**. Front: `BannerAssinatura`, modo somente-leitura (desabilita Nova obra/Novo RDO), seção **Assinaturas** no console. **Migrations `Assinatura` + `RedefinicaoSenha` já geradas** (commit de billing na main).

## Branches em revisão (ainda NÃO na main) — mergear nesta ordem
1. **`feat/regras-he-por-empresa`** (base: `feat/hh-semanal`, já na main) — taxas de HE **configuráveis por empresa**: entidade `RegraHoraExtra` (BaseEntity; **defaults = comportamento atual**), `ConfiguracoesController` (`GET/PUT /configuracoes/hora-extra`, Gestor/Admin), `HhSemanalCalculo` passa a **receber a regra** e devolve `extras:[{percentual,horas}]` (baldes **dinâmicos** por %), corte de sábado por horário; aba **Configurações › Horas extras**. ⚠️ **MUDA o contrato** do endpoint HH (`extras[]` no lugar de `extra50/70/100/150HH` — `HhSemanal.tsx` atualizado junto). **Migration a gerar: `regras_hora_extra`.** Sem config salva, cai no default (`new RegraHoraExtra()`), então nada muda até a empresa configurar. Conflito esperado em `AppDbContext`/`Home.tsx`/`HhSemanal*`.
2. **`offline-first`** — PWA/IndexedDB: cache-through nos GET + **fila de sync** do RDO (autosave enfileira offline, last-write-wins; reenvia ao voltar online / a cada 30s / no load; 401/403 não descarta), `OfflineIndicator`, service worker `navigateFallback`. Escopo v1: **editar** um RDO já em cache offline. Testar no navegador (`npm run build && npm run preview`). Conflito esperado em `api.ts`/`App.tsx`/`main.tsx`/`Rdo.tsx`. Sem migration.

## Regras de negócio já confirmadas pelo usuário
- **Hora extra** (jornada única por RDO): expediente seg–sex 07:00–16:48 = 8h48 (almoço descontado pelo horário real). Extra dia útil = após 16:48 (**50% até 10h/semana, 70% acima** — aplicar no fechamento semanal). Sáb/Dom/**Feriado** (checkbox) = **100% até 8h, 150% acima**. Cálculo no backend em `Domain/Services/JornadaCalculo.cs` (e espelhado no front em `Rdo.tsx`).
- **RBAC:** gerir Obra/Contrato/EAP/Clientes/importar/vincular/faturamento + ver valores R$ + ver/editar RDOs de TODAS as obras = **Planejador + Gestor + Admin**. Criar usuário = só Gestor+Admin. Encarregado/Líder/Supervisor: só obras vinculadas, sem R$.
- **Faturamento:** configurável por contrato (eventos: assinatura/entrada/mobilização/canteiro mensal/medição/comissionamento/entrega; condições 21/42, 30/45/60/180, à vista). Não implementado ainda (Sprint 7).
- **Avanço do item** = max(concluído?1, %informado, qtd_exec/qtd_prev). **Farol** = amarelo 10–25, vermelho >25 (a confirmar/configurável).

## Pendências de decisão do usuário
- **Fechamento HH — acúmulo 50/70:** implementado como **por pessoa/semana** (as 1ªs 10h extras de dia útil de cada pessoa a 50%, resto a 70%); a jornada é única por RDO e só há a contagem do efetivo, então usa-se uma "pessoa representante" x nº de pessoas do dia. Comentado no código (`HhSemanalCalculo`). **Confirmar com o dono.**
- Assinaturas: quem assina (Encarregado sempre? + Fiscal? + Supervisor?).
- Logo da empresa no PDF (opcional — usuário enviaria um PNG).

## Como rodar (nova máquina)
Pré-requisitos: **.NET 10 SDK**, **Node 20+**, **PostgreSQL 16+** (ou Docker), **dotnet-ef** (`dotnet tool install --global dotnet-ef --version 10.0.0`).

1. **Banco:** criar role/db (senha usada no dev = `industrialos`):
   ```sql
   CREATE ROLE industrialos LOGIN PASSWORD 'industrialos';
   CREATE DATABASE industrialos OWNER industrialos;
   ```
   Ajustar `backend/src/IndustrialOS.Api/appsettings.json` (ConnectionStrings:Postgres) se a senha do seu Postgres for outra.
2. **Segredos R2 (não versionados):** criar `backend/src/IndustrialOS.Api/appsettings.Local.json` com:
   ```json
   { "R2": { "AccountId": "<...>", "AccessKeyId": "<...>", "SecretAccessKey": "<...>", "Bucket": "industrialos" } }
   ```
   (As chaves do R2 ficam com o usuário — bucket Cloudflare `industrialos`, endpoint `<AccountId>.r2.cloudflarestorage.com`.)
3. **Backend:** `cd backend && dotnet run --project src/IndustrialOS.Api` (aplica migrations e faz o seed: tenant demo + admin `admin@demo.com`/`admin123`, + Planejador de teste `maria@demo.com`/`maria123` se recriado, + Encarregado `joao@demo.com`/`joao123`, + 61 funções).
4. **Frontend:** `cd frontend && npm install && npm run dev` → http://localhost:5173.

## Convenções importantes
- Filhos owned do RDO (efetivo, paralisações, recursos, serviços, retrabalho) **não podem ter `Id = Guid.NewGuid()` no construtor** (o EF trataria como update → `DbUpdateConcurrencyException`). O Id fica sem default; EF gera.
- R2 com AWS SDK v4: no `AmazonS3Config` usar `ForcePathStyle=true`, `AuthenticationRegion="auto"`, `RequestChecksumCalculation=WHEN_REQUIRED`, `ResponseChecksumValidation=WHEN_REQUIRED`; no `PutObjectRequest` usar `DisablePayloadSigning=true`.
- Autosave do RDO (`Rdo.tsx`) faz PUT full-replace — a UI **precisa enviar TODAS as seções** senão zera as omitidas.
- Enums da API serializados como string (JsonStringEnumConverter). Provider do banco fixo em Postgres.
